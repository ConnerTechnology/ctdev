# Personal development workstation: shell, editors, runtimes, containers,
# backups, and disk health.

description = "Development workstation (shell, runtimes, containers, CLI tools)"

components = [
  "zsh",
  "git",
  "gh",
  "git-spice",
  "mosh",
  "tmux",
  "fonts",
  "vscode",
  "node",
  "go",
  "docker",
  "tailscale",
  "claude-code",
  "jq",
  "btop",
  "shellcheck",
  "restic",
  "smartmontools",
]

configure = ["autoupdate"]

notes = """
Next steps (interactive):
  gh auth login                    authenticate GitHub
  ctdev configure git              name, email, SSH signing key
  ctdev configure restic           backup repo, credentials, healthcheck URL
  ctdev backup paths               pick what restic backs up
  sudo tailscale up                join the tailnet
Walk the OS settings when convenient: ctdev configure
"""
