[Unit]
Description=Daily restic backup of this machine

[Timer]
OnCalendar=*-*-* 03:30:00
# Run on next boot if the machine was off at the scheduled time.
Persistent=true
# Spread load if several machines share a schedule.
RandomizedDelaySec=5m

[Install]
WantedBy=timers.target
